# ferramentas/

Scripts rodados **à mão**, de vez em quando. Nada aqui é chamado pelo pipeline
diário nem importado por `app/`. Movidos de `app/` em 03/09/2026 (Fase 0 da
`SIMPLIFICACAO.md` do portal) pra separar o que roda todo dia do que é ferramenta.

`_arquivo/` guarda o que foi **substituído** (ex.: Google Ads via Windsor, que virou
`app/google_ads_api.py`). Fica pra consulta; não roda.
